<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Tasks</title>
</head>
<body>
    <h1>Your New Tasks</h1>
    <ul>
        <% tasks.forEach(task => { %>
            <li>
                <strong>Description:</strong> <%= task.description %><br>
                <strong>Due Date:</strong> <%= new Date(task.date).toLocaleDateString() %><br>
                <strong>Class:</strong> <%= task.class.name %><br>
            </li>
        <% }); %>
    </ul>
    <button onclick="window.location.href='/students/student-dashboard'">Back to Dashboard</button>
</body>     
</html>
