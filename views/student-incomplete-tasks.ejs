<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Incomplete Tasks</title>
</head>
<body>
    <h1>Your Incomplete Tasks</h1>
    <form action="/mark-tasks-complete" method="post">
        <ul>
            <% tasks.forEach(task => { %>
                <li>
                    <strong>Description:</strong> <%= task.description %><br>
                    <strong>Due Date:</strong> <%= new Date(task.date).toLocaleDateString() %><br>
                    <label>
                        <input type="checkbox" name="completedTasks" value="<%= task._id %>"> Mark as Complete
                    </label>
                </li>
            <% }); %>
        </ul>
        <button type="submit">Submit Changes</button>
    </form>
</body>
</html>
